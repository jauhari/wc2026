/** ISR revalidate untuk halaman (detik). */
export const PAGE_REVALIDATE = 30

/** Cache data turnamen di memory Worker (ms). */
export const TOURNAMENT_CACHE_TTL_MS = 30_000

/** Cache fetch openfootball remote (ms). */
export const OPENFOOTBALL_CACHE_TTL_MS = 60_000

/** Cache overlay live FIFA (ms). */
export const OVERLAY_CACHE_TTL_MS = 30_000

/** Interval auto-refresh client saat ada pertandingan live (ms). */
export const CLIENT_REFRESH_LIVE_MS = 15_000

/** Interval auto-refresh client normal (ms). */
export const CLIENT_REFRESH_MS = 30_000

/** Timeout fetch overlay live FIFA (ms). */
export const OVERLAY_TIMEOUT_MS = 1_500