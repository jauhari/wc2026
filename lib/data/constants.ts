/** ISR revalidate untuk halaman (detik). */
export const PAGE_REVALIDATE = 120

/** Cache data turnamen di memory Worker (ms). */
export const TOURNAMENT_CACHE_TTL_MS = 90_000

/** Halaman detail — revalidate lebih lama (statis). */
export const DETAIL_PAGE_REVALIDATE = 300

/** Timeout fetch overlay live FIFA (ms). */
export const OVERLAY_TIMEOUT_MS = 1_500